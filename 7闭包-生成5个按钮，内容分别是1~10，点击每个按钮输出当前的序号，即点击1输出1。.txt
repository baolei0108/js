//生成了5个按钮
for(var i=1;i<=5;i++) {
    var doc = document.createElement('button');
    doc.innerHTML=i;

    doc.onclick = function(x) {
        return function(){
            console.log(x);
        }

    }(i);

    document.body.appendChild(doc);
}
