事件捕获：
从根元素--->一直捕获到目标元素
addEventListener("click", function(e){

事件冒泡：
从目标元素--->一直捕获到根元素

注意：
如是目标元素，则遵循：先绑定的事件先发生
若不是目标元素，则遵循：先发生捕获后发生冒泡


阻止冒泡方法：
e.preventDefault()可以阻止事件的默认行为发生
e.stopPropagation()


事件委托（事件代理）：（利用冒泡原理--因为事件冒泡模型被所有主流的浏览器兼容）
好处：可以给新注册的元素添加事件

